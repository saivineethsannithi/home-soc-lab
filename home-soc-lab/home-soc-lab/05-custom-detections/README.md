# Phase 5 — Custom Detection Engineering (planned)
Writing Sigma rules for gaps in Wazuh's default ruleset. Each rule will have a writeup: what it detects, why the default missed it, false positive analysis.
